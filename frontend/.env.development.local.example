# Frontend development environment
# Set dev port to avoid conflict with backend (backend uses :3000 by default)
PORT=3001

# Backend API base URL used by frontend (adjust if backend runs on different port/host)
REACT_APP_API_URL=http://localhost:3000/api
